<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="backgroundColor">@color/primaryLightColor</item>
    </style>

    <!-- Style for preference page-->
    <style name="SettingsFragmentStyle">
        <item name="colorPrimary">#bcbbbd</item>
        <item name="colorPrimaryDark">#f2eff2</item>
        <item name="colorAccent">#eeedef</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="backgroundColor">#eeedef</item>
    </style>


    <!-- Expanded Toolbar DrinkClicked -->
    <style name="ExpandedAppBar" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/textColor</item>
    </style>

    <!--Creates divider. -->
    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">?android:attr/listDivider</item>
        <item name="android:layout_margin">4dp</item>
    </style>

    <!-- Style is for the splash screen. -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Alert Dialog. -->
    <style name="AlertDialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Button Style -->
    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button" />


</resources>
